# Personality test back 

HAL explorer is available on `http://localhost:8080/explorer/index.html`.
Then enter e.g. `http://localhost:8080/tests`.

Swagger is available on `http://localhost:8080/swagger-ui/index.html`.
